# CodeBuddy progress

## 2026-06-20

- Chose `CodeBuddy` as the first user-level custom agent.
- Chose `/checkin` as the recurring prompt.
- Decided to keep reusable agent files in `C:\project\ai-agent`.
- Decided to keep project-specific memory in the project repo instead of a separate global memory store.
- Defined the first MVP around repo-aware coaching with lightweight local memory.
