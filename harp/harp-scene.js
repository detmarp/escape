export default class HarpScene {
  constructor(appScene, program) {
    this.appScene = appScene;
    this.program = program;
  }

  run() {
    // Add program-specific scene logic here
  }
}
